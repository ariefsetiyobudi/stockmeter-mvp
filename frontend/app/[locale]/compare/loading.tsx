import { ComparisonSkeleton } from '@/components/SkeletonLoader';

/**
 * Loading state for comparison page
 * Requirements: All - Loading states for route transitions
 */
export default function Loading() {
  return <ComparisonSkeleton />;
}
