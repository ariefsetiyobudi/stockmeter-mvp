import { WatchlistSkeleton } from '@/components/SkeletonLoader';

/**
 * Loading state for watchlist page
 * Requirements: All - Loading states for route transitions
 */
export default function Loading() {
  return <WatchlistSkeleton />;
}
